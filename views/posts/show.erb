<p class="post">
  <h1 class="post_title"><%= @post.title %></h1>

  <p class="post_body">
    <%= @post.body %>
  </p>

  <ul class="comments">
    <% @post.comments.each do |comment| %>
      <li class="comment">
        <p class="comment_body"><%= comment.body %></p>
        <p class="comment_author">by <%= comment.user.username %></p>
        <p class="comment_created_at">at <%= comment.created_at %></p>
      </li>
    <% end %>
  </ul>
  
  <form action="/comments/create" method="POST">
    <% if @username %>
      <input type="hidden" name="username" value="<%= @username %>" />
    <% else %>
      <input type="hidden" name="username" value="anonymous" />
    <% end %>
    <input type="hidden" name="post_id" value="<%= @post.id %>">
    <textarea name="body"></textarea>
    <input type="submit" value="add comment" />
    <br />
    <input type="radio" name="codegroup" value="text" checked>Text<br>
    <input type="radio" name="codegroup" value="ruby">Ruby<br>
    <input type="radio" name="codegroup" value="html">HTML<br>
    <input type="submit" value="Post" />
  </form>
</p>
<a href="/posts">back</a>